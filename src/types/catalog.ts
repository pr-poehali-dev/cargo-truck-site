
export interface FilterParams {
  search?: string;
  category?: string;
  brand?: string;
  minPrice?: number;
  maxPrice?: number;
  sort?: string;
  inStock?: boolean;
  isNew?: boolean;
  isDiscount?: boolean;
}
